---
name: structural-linter
on:
  push:
    branches: [main]
jobs:
  lint:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Lint
        run: yamllint -c .yamllint.yml .
